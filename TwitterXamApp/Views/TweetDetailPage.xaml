<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="TwitterXamApp.TweetDetailPage">
	<ContentPage.Content>
	<Grid Padding="10">
	<Grid.ColumnDefinitions>
	<ColumnDefinition />
	<ColumnDefinition />
	</Grid.ColumnDefinitions>
	<Grid.RowDefinitions>
	<RowDefinition Height="Auto"/>
	<RowDefinition Height="Auto"/>
	<RowDefinition />
	</Grid.RowDefinitions>
	<StackLayout Orientation="Horizontal" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
	<Image Source="{Binding ImageUri}" />
	<Label Text="{Binding Name}" Style="{DynamicResource TitleStyle}" FontSize="Medium"  />
	
	</StackLayout>

	<Label Text="{Binding Value}" FontSize="Medium" Grid.Row="2" LineBreakMode="WordWrap" Grid.Column="0" Grid.ColumnSpan="2"/>

	</Grid>
	</ContentPage.Content>
</ContentPage>
